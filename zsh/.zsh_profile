
# Set personal aliases, overriding those provided by oh-my-zsh libs,
# plugins, and themes. Aliases can be placed here, though oh-my-zsh
# users are encouraged to define aliases within the ZSH_CUSTOM folder.
# For a full list of active aliases, run `alias`.
#
# Example aliases
alias zshconf="nvim ~/.zsh_profile"
alias asus="ssh -p 717 192.168.0.17"
alias work="cd ~/workspace"
alias cod="codium"
alias py="python3"
alias python="python3"
alias czsh="source ~/.zshrc"
alias glow="git flow"
alias getch="git fetch --prune"
alias gull="git pull"
alias gush="git push"
alias neofetch='fastfetch'
alias fetch='fastfetch'
alias vim="nvim"

addToPathFront() {
    if [[ ":$PATH:" != *":$1:"* ]]; then
        export PATH="$1:$PATH"
    fi
}

export JAVA_HOME="/usr/lib/jvm/jdk-22-oracle-aarch64"
export NVIM_HOME="$HOME/workspace/neovim"
export NODE_HOME="$HOME/workspace/cloned_repos/node-v20.12.2-linux-x64"
export DEPOT_TOOLS="$HOME/workspace/cloned_repos/depot_tools"

# All PATHS that might be needed
addToPathFront $HOME/.local/bin
addToPathFront $JAVA_HOME/bin
addToPathFront $NVIM_HOME/bin
addToPathFront $DOTNET_ROOT
addToPathFront $DOTNET_ROOT/tools
addToPathFront $NODE_HOME/bin
addToPathFront $DEPOT_TOOLS

# bind to start tmux fzf
bindkey -s ^f "tmux-sessionizer\n"

# To make github private key permanent
if [ -z "$SSH_AUTH_SOCK" ]; then
    eval "$(ssh-agent -s)" > /dev/null 2>&1
    ssh-add ~/.ssh/github > /dev/null 2>&1
    ssh-add ~/.ssh/asusV2 > /dev/null 2>&1
fi

eval "$(oh-my-posh init zsh --config $HOME/.config/ohmyposh/theme.toml)"
